python3.11 -m venv env_RectifiedTraj
source env_RectifiedTraj/bin/activate
hash -r 2>/dev/null || true

# System dependency for map slicing (required by parquet_processor map slicer):
# Ubuntu/Debian:
sudo apt-get update && sudo apt-get install -y osmium-tool
# Arch Linux:
sudo pacman -S --needed osmium-tool

python -m pip install -U pip setuptools wheel
python -m pip install -r requirements.txt
python -m pip check
